{
  "name": "idea-lab-email-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "BullMQ worker for OTP emails (uses ../backend/src for SMTP helpers)",
  "dependencies": {
    "bullmq": "^5.34.5",
    "dotenv": "^17.2.4"
  }
}
