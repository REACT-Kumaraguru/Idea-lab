# Redis for BullMQ (OTP email queue)
# Tune memory on small VPS; BullMQ uses Redis streams.

maxmemory 256mb
maxmemory-policy allkeys-lru

# Persistence (optional; queue jobs are ephemeral — keep AOF light)
appendonly yes
appendfsync everysec

tcp-keepalive 300
timeout 0
